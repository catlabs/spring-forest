type Animal {
  id: ID!
  name: String!
  cutness: Int!
  intelligence: Int!
}

type Food {
  id: ID!
  name: String!
}

type Query {
  findAllAnimals: [Animal]!
  findByName(name: String!): Animal
  foods: [Food]!
}